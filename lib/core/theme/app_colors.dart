import 'package:flutter/material.dart';

/// Quest Design System Color Palette
/// Defined in DESIGN.md
class AppColors {
  AppColors._();

  // Primary
  static const Color questBlue = Color(0xFF2563EB);

  // Secondary
  static const Color midnightSlate = Color(0xFF0F172A);

  // Surface
  static const Color surface = Color(0xFF111827);

  // Background
  static const Color background = Color(0xFF030712);

  // Cards
  static const Color card = Color(0xFF1F2937);

  // Borders
  static const Color border = Color(0xFF374151);

  // Accent
  static const Color auroraPurple = Color(0xFF7C3AED);

  // Success
  static const Color emerald = Color(0xFF10B981);

  // Warning
  static const Color amber = Color(0xFFF59E0B);

  // Danger
  static const Color crimson = Color(0xFFEF4444);

  // Information
  static const Color skyBlue = Color(0xFF38BDF8);

  // XP / Gamification
  static const Color gold = Color(0xFFFBBF24);

  // Text
  static const Color textPrimary = Colors.white;
  static const Color textSecondary = Color(0xFF9CA3AF);
  static const Color textMuted = Color(0xFF6B7280);
}
