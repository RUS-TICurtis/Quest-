## 2024-05-18 - Missing Tooltips on IconButtons
**Learning:** Found that `IconButton` widgets in chat and messaging screens lacked `tooltip` properties, which hinders accessibility and the user experience for those relying on hover states or screen readers.
**Action:** Always add `tooltip` properties to icon-only buttons to ensure clear intent is conveyed to all users.
